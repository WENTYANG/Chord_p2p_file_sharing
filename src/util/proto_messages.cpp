#include "proto_messages.h"

using namespace std;
using namespace p2pfilesharing;

LookupFileResponse generate_lookup_response(bool if_exist, bool owner_host_name, bool owner_port) {
  LookupFileResponse rsp;
  return rsp;
}
